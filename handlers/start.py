from aiogram import Router, F
from aiogram.types import Message
from keyboards.main import main_kb

router = Router()

@router.message(F.text == "/start")
async def start_handler(message: Message):
    await message.answer(
        "👋 *Добро пожаловать!*\n\n"
        "🤖 Это умный автоматизированный сервис публикации объявлений.\n\n"
        "📢 Вы отправляете пост —\n"
        "🧠 система автоматически проверяет его —\n"
        "🚀 пост публикуется в официальном канале,\n"
        "откуда он автоматически попадает в группу.\n\n"
        "✅ *Публикация бесплатная*\n"
        "🚫 Запрещённый контент не допускается\n\n"
        "⬇️ Нажмите кнопку ниже",
        reply_markup=main_kb(),
        parse_mode="Markdown"
    )
